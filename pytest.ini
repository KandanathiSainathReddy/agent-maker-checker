[pytest]
; repo root on sys.path so `proxy.*` imports resolve the same way in tests as in
; the app itself (see proxy/app.py's upstream import for why that prefix matters).
pythonpath = .
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
