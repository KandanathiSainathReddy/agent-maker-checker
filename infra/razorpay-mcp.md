# Razorpay MCP server — self-hosting notes

Filled in during Phase 2: exact image/build steps for Razorpay's open-source MCP
server, transport choice (stdio vs SSE/HTTP), test-key setup, and the proof that a
proxy-allowed action lands in the real Razorpay test dashboard.
