{
  "name": "dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "agent-maker-checker spend-control dashboard — Vite static app for Amplify Hosting",
  "scripts": {
    "dev": "vite --host --port 3000",
    "build": "vite build",
    "preview": "vite preview --host --port 3000"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
